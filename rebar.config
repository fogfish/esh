{erl_opts, [
   %native, {hipe, o3},
]}.

{lib_dirs, [".", "deps"]}.
{cover_enabled, true}.

{deps, [
   % process library
   {plib, ".*",
      {git, "http://github.com/kfsm/plib", {branch, master}}
   }

  % process pipe
  ,{pipe, ".*",
      {git, "http://github.com/kfsm/pipe", {branch, master}}
   }
]}.
